import 'package:flutter/material.dart';

class CompositionsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Composiciones'),
      ),
      body: Center(
        child: Text('Contenido de Composiciones aquí'),
      ),
    );
  }
}